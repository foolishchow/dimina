# Acceptance — fe-tools-view-parse-walk-ctx-deep

## A-VPD-1 — compileModule 加 ctx 透传 + A2 deviation 撤回 ✓ done

## A-VPD-2 — tryModuleCache/mergeWxsModules/collectAllWxsModules 加 ctx ✓ done

## A-VPD-3 — toCompileTemplate/loadTemplates 跨层透传 ✓ done

## A-VPD-4 — 行为 0 ✓ done

tsc 0 + vitest 88/88（3 flaky solo pass）+ 7-diff=0。
