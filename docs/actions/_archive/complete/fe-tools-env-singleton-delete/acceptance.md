# Acceptance — fe-tools-env-singleton-delete

## A-ESD-1 — packer 层 getter caller 迁 state.graph ✓ done（前置 A0-A5a）

## A-ESD-2 — runtime 改候选 b ⏸ 阻塞（component 不可序列化）

## A-ESD-3 — storeInfo wrapper compat 写删 ⏸ 阻塞

## A-ESD-4 — env.ts singleton 删 ⏸ 阻塞（component/resolveAlias 须 ALS）

## A-ESD-5 — 行为 0 n/a（无代码改动——阻塞确认）
